# Monthly Audit — 2026-06-29 (sudoku, interactiveResume)

## Executive Summary

- **sudoku**: Build passes ✅. No lint script. Five accessibility gaps (missing ARIA labels on grid cells being the most critical), one logic bug (hint reveals cells from a mismatched board), and several magic-number/comment violations. Deps are stale — TypeScript is 2 major versions behind, `@types/node` is 10 major versions behind.
- **interactiveResume**: No build system. JS syntax is valid ✅. Three critical A11Y failures (SVG charts have no accessible descriptions, interactive D3 elements have no keyboard support). One missing script file (`hexbinChart.js` referenced but absent — 404 in production). Several `console.log` statements and undeclared globals left in production code.

---

## 1. Skill Compliance

### 🚨 Critical

**A11Y [severity: critical]: sudoku/src/components/SudokuBoard.tsx:66-74 — All 81 `<input>` cells have no `aria-label` — screen readers announce each as a generic "edit text" with no positional context — add `aria-label={`Row ${rowIndex + 1}, Column ${colIndex + 1}`}` to each cell**

**A11Y [severity: critical]: interactiveResume/index.html:33 + ganttChart.js — Skills SVG has no `aria-label` or `<title>` element — the entire hexbin visualization is invisible to assistive technology — add `<title>Skills hexbin chart</title>` inside the `<svg>` and `aria-labelledby` pointing to it**

**A11Y [severity: critical]: interactiveResume/index.html:25 + ganttChart.js — Gantt chart SVG has no accessible name or description — the work-history timeline is opaque to screen readers — add a `<title>Work experience timeline</title>` inside the appended SVG or an `aria-label` on `#ganttContainer`**

**A11Y [severity: critical]: interactiveResume/ganttChart.js:280-444 — All D3 click interactions on SVG `<g>` elements have no keyboard equivalent — role selection is mouse-only — add `tabindex="0"` to each bar `<g>` and handle `keydown` Enter/Space events**

---

### 🔴 High

**QUALITY [severity: high]: sudoku/src/components/SudokuBoard.tsx:8 — `solution` is initialized with a separate `generateSudoku(0)` call — since `generateSolvedBoard()` is random, the "solution" board is a completely different puzzle than `initialBoard`, so hints fill in wrong numbers — refactor so `generateSudoku` returns both the puzzle and its solution together, or derive the solution inside `generateSudoku` and return both**

**ANTI-PATTERN: interactiveResume/ganttChart.js:84 — `id="hexbinTooltip"` is set on every hexagon path in a loop — duplicate IDs are invalid HTML and break `getElementById` lookups — change to a class: `class="hexbinTooltip"`**

**A11Y [severity: high]: interactiveResume/index.html:62 — `<script src="hexbinChart.js">` references a file that does not exist in the repository — this generates a 404 on page load, which may block JS execution — remove the dead script tag or restore the missing file**

**QUALITY [severity: high]: interactiveResume/ganttChart.js:194,282,420-421 — Three `console.log` statements left in production code (lines 194 `console.log(width)`, 195 `console.log(height)`, 282 `console.log(bigRects)`, 420 `console.log(this)`, 421 `console.log(d3.select(this).text())`) — remove all**

---

### 🟡 Medium

**A11Y [severity: medium]: sudoku/src/components/SudokuBoard.tsx:61 — `.sudoku-grid` `<div>` has no `role="grid"` — the 9×9 layout is not announced as a grid structure to screen readers — add `role="grid"` and `aria-label="Sudoku puzzle"`**

**A11Y [severity: medium]: sudoku/src/App.tsx:6 — Page body has no `<main>` landmark — all content is in a plain `<div>` — wrap the `<SudokuBoard />` in `<main>`**

**A11Y [severity: medium]: interactiveResume/index.html — No skip-to-content link — keyboard users must tab through the entire header before reaching the interactive content — add `<a href="#vizContainer" class="skip-link">Skip to content</a>` as the first body element**

**ANTI-PATTERN: interactiveResume/ganttChart.js:4,13,35-36,296,298,384 — Multiple undeclared global variables (`latestRole`, `skillsData`, `skillsContainerWidth`, `skillsContainerHeight`, `vals`, `thisRole`) — implicit globals pollute `window` and cause strict-mode errors — declare all with `var`/`let`/`const`**

**ANTI-PATTERN: interactiveResume/ganttChart.js:55,73 — `var skillsSvg` is declared twice — the second declaration at line 73 silently overwrites the first — remove the duplicate declaration at line 73 and keep the assignment only**

**QUALITY [severity: medium]: interactiveResume/style.css:162 — Bare `#` on its own line is an invalid CSS rule — causes a CSS parse error — remove the stray character**

**ANTI-PATTERN: interactiveResume/ganttChart.js:190 — Magic numbers `var w = 1200` and `var h = 250` are repeated throughout the file with no named constant — extract to `const GANTT_WIDTH = 1200` and `const GANTT_HEIGHT = 250`**

**A11Y [severity: medium]: interactiveResume/index.html:40-44 — Role, company, location `<p>` elements have no semantic label — their purpose is only clear from context — add a `<span class="visually-hidden">` prefix like "Company:" or use `aria-label` on each `<p>`**

**A11Y [severity: medium]: sudoku/src/components/SudokuBoard.tsx:72 — Cells with pre-filled numbers use `disabled` — screen readers typically skip `disabled` inputs entirely — use `readOnly` with `aria-readonly="true"` instead so all cells remain focusable and readable**

---

### 🟢 Low

**A11Y [severity: low]: sudoku/src/components/SudokuBoard.tsx:81 — "Remaining Hints" count updates silently — screen readers won't announce the change — wrap in an `aria-live="polite"` region**

**QUALITY [severity: low]: sudoku/src/components/SudokuBoard.tsx:11-20 — Multiple inline comments explain obvious code (e.g. `// Track remaining hints`, `// Reset hint count`) — delete all comments that restate what the variable name already says**

**ANTI-PATTERN: sudoku/src/components/SudokuBoard.tsx:55 — Magic number `3000` (ms) in `setTimeout` — extract to a named constant `const HINT_HIGHLIGHT_DURATION_MS = 3000`**

**ANTI-PATTERN: sudoku/src/components/SudokuBoard.tsx:10 — Magic number `3` for initial hint count — extract to `const INITIAL_HINT_COUNT = 3`**

**QUALITY [severity: low]: interactiveResume/ganttChart.js:10 — Code comment referencing IDE shortcut (`// Opt + Cmd + Arrow Keys`) left in production file — remove editor notes from committed code**

**QUALITY [severity: low]: interactiveResume/index.html:28 — Commented-out HTML `<!--div id="tag"></div-->` — remove dead markup**

**QUALITY [severity: low]: interactiveResume/style.css:196-220 — Large block of commented-out CSS for `#tag` tooltip — remove dead styles**

**QUALITY [severity: low]: interactiveResume/ganttChart.js:275,336 — Commented-out code (`.attr("class", "balance")`, `.style("color", "#6666ff")`) — remove**

---

## 2. Build / Lint Health

| Repo | Step | Result | Notes |
|---|---|---|---|
| sudoku | `npm install` | ✅ | Clean install |
| sudoku | `npm run build` | ✅ | Compiled successfully; gzip: 59.71 kB JS, 1.77 kB chunk, 667 B CSS |
| sudoku | `npm run lint` | ❌ | Script does not exist — add `"lint": "react-scripts lint"` or integrate ESLint directly |
| interactiveResume | npm (no package.json) | N/A | Plain HTML/JS project, no build system |
| interactiveResume | `node --check ganttChart.js` | ✅ | JS syntax valid |

**Note on browserslist warning:** Build emits `"Browserslist: browsers data is 16 months old"` — run `npx update-browserslist-db@latest` in the sudoku repo.

---

## 3. Dependency Updates

All packages are from the **sudoku** repo. **interactiveResume** has no package.json (D3 is loaded via CDN — d3 v5 is currently on v7; CDN URL should be updated).

| Package | Current | Wanted | Latest | Versions Behind | Priority |
|---|---|---|---|---|---|
| @types/node | 16.18.126 | 16.18.126 | 26.0.1 | **10 major** | 🔴 HIGH |
| typescript | 4.9.5 | 4.9.5 | 6.0.3 | **2 major** | 🔴 HIGH |
| @types/jest | 27.5.2 | 27.5.2 | 30.0.0 | **3 major** | 🔴 HIGH |
| web-vitals | 2.1.4 | 2.1.4 | 5.3.0 | **3 major** | 🔴 HIGH |
| @testing-library/user-event | 13.5.0 | 13.5.0 | 14.6.1 | 1 major | 🟡 MEDIUM |
| @testing-library/dom | 10.4.0 | 10.4.1 | 10.4.1 | patch | 🟢 LOW |
| @testing-library/jest-dom | 6.6.3 | 6.9.1 | 6.9.1 | patch | 🟢 LOW |
| @testing-library/react | 16.2.0 | 16.3.2 | 16.3.2 | patch | 🟢 LOW |
| @types/react | 19.0.10 | 19.2.17 | 19.2.17 | patch | 🟢 LOW |
| @types/react-dom | 19.0.4 | 19.2.3 | 19.2.3 | patch | 🟢 LOW |
| react | 19.0.0 | 19.2.7 | 19.2.7 | patch | 🟢 LOW |
| react-dom | 19.0.0 | 19.2.7 | 19.2.7 | patch | 🟢 LOW |

**Patch upgrades** (`@testing-library/*`, `react`, `react-dom`, `@types/react*`) are safe to bundle as a single `npm update` run.

---

## 4. Dead Routes

Neither project uses Next.js. Checked hardcoded internal resource references:

**404 RISK: `hexbinChart.js` in interactiveResume/index.html:62 — `<script src="hexbinChart.js">` — file does not exist in the repository; the hexbin chart code lives in `ganttChart.js` — remove this dead script tag to eliminate a 404 on every page load**

No broken internal `<a href>` links found in either project.

---

## 5. Top 5 Action Items

1. **Fix the hint bug in sudoku** (`SudokuBoard.tsx:8`) — hints currently reveal numbers from a *different* randomly generated board, making them wrong. Refactor `generateSudoku` to return `{ puzzle, solution }` so both share the same underlying board. This is a gameplay-breaking correctness bug.

2. **Add `aria-label` to all sudoku input cells** (`SudokuBoard.tsx:66`) — 81 inputs are completely anonymous to screen readers. One-line fix: add `aria-label={`Row ${rowIndex + 1}, Column ${colIndex + 1}`}`.

3. **Remove dead `<script src="hexbinChart.js">` from interactiveResume** (`index.html:62`) — generates a 404 on every page load. The hexbin code is already in `ganttChart.js`. Delete the tag.

4. **Add keyboard support to D3 Gantt bars in interactiveResume** — the entire interactive resume is mouse-only. Add `tabindex="0"` + `keydown` handler (Enter/Space) to each bar `<g>` element inside `drawRects`.

5. **Update high-priority stale deps in sudoku** — `@types/node` is 10 major versions behind, TypeScript is on v4 while v6 is current, `@types/jest` and `web-vitals` are 3 major versions behind. Run `npm install typescript@latest @types/node@latest @types/jest@latest web-vitals@latest` and validate the build.
